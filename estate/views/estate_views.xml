<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="estate_model_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="context">{'search_default_available': True}</field>
    </record>
    <!-- TREE VIEW -->
    <record id='estate_view_list' model='ir.ui.view'>
        <field name='name'>estate.view.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="tree_view"
                decoration-success="state in ('offer_accepted','offer_recieved')"
                decoration-bf="state == 'offer_accepted'" decoration-muted="state=='solded'">

                <header>
                    <button name="make_offer" class="oe_highlight" type="object" string="ADD OFFER" />
                </header>

                <field name="name" />
                <field name="property_type_id" />
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                <field name="postcode" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="date_availability" optional="hide" />
                <field name="state" invisible="1" />
            </tree>
        </field>
    </record>

    <!-- FORM VIEW -->

    <record id="estate_form_view" model="ir.ui.view">
        <field name="name">estate.form.view</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="form_view">
                <header>
                    <button name="solded" type="object" string="sold"
                        attrs="{'invisible':[('state','in',['solded','cancelled'])]}" />
                    <button name="cancelled" type="object" string="Cancel"
                        attrs="{'invisible':[('state','in',['solded','cancelled'])]}" />
                    <field name="state" widget="statusbar" />
                </header>
                <sheet>
                    <h1>
                        <field name="name" />
                    </h1>
                    <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                    <group>
                        <group>
                            <group>
                                <field name="property_type_id" string="Property Type"
                                    options="{'no_create': True}" />
                                <field name="postcode" />
                                <field name="date_availability" />

                            </group>
                            <group>
                                <field name="image" class="oe_avatar" widget="image"></field>
                                <field name="expected_price" />
                                <field name="best_price" string="best price" />
                                <field name="selling_price" />

                            </group>
                        </group>
                        <notebook>
                            <page string="Description">
                                <group>
                                    <field name="description" />
                                    <field name="state" />
                                    <field name="bedrooms" />
                                    <field name="living_area" />
                                    <field name="facades" />
                                    <field name="garage" />
                                    <field name="garden" />
                                    <field name="garden_area"
                                        attrs="{'invisible': [('garden', '=', False)]}" />
                                    <field name="garden_orientation" string=" Garden Orientation"
                                        attrs="{'invisible': [('garden', '=', False)]}" />
                                    <field name="total_area" />
                                </group>
                            </page>

                            <page string="offer">
                                <field name="offer_ids"
                                    attrs="{'readonly':[('state','in',['offer_accepted','sold','canceled'])]}" />
                            </page>
                            <page string="Other Info">
                                <group>
                                    <field name="buyer_id" />
                                    <field name="partner_id" />
                                </group>
                            </page>
                        </notebook>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers" />
                    <field name="message_ids" widget="mail_thread" />
                    <field name="activity_ids" widget="mail_thread" />
                </div>
            </form>
        </field>
    </record>

    <!-- kanban view: -->
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="false">
                <field name="color_kanban" />
                <field name="image"></field>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="o_kanban_image" style="height:100px width:100px">
                                <img
                                    t-att-src="kanban_image('estate.property','image',record.id.raw_value)" />
                            </div>
                            <div class="o_dropdown_kanban dropdown">
                                <a class="dropdown-toggle o-no-caret btn" role="button" data-bs-toggle="dropdown" data-bs-display="static" href="#" aria-label="Dropdown menu" title="Dropdown menu">
                                    <span class="fa fa-ellipsis-v"/>
                                </a>
                                <div class="dropdown-menu" role="menu">
                                    <t t-if="widget.editable"><a role="menuitem" type="edit" class="dropdown-item">Edit</a></t>
                                    <t t-if="widget.deletable"><a role="menuitem" type="delete" class="dropdown-item">Delete</a></t>
                                    <ul class="oe_kanban_colorpicker" data-field="color"/>
                                </div>
                            </div>
                            <field name="state" invisible="1" />
                            <div>
                                <field name="name" />
                                <div> Expected price: <field name="expected_price" />
                                </div>
                            </div>
                            <div t-if="record.state.raw_value == 'offer_received'"> Best price: <field
                                    name="best_price" />
                            </div>
                            <div t-if="record.state.raw_value == 'offer_accepted'"> Selling price: <field
                                    name="selling_price" />
                            </div>
                            <div>
                                <field name="tag_ids" options="{'color_field': 'color'}" />
                            </div>


                        </div>
                    </t>
                </templates>
                <field name="id" attrs="{'invisible': True}" />

            </kanban>

        </field>
    </record>


    <!-- SEARCH VIEW -->
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="estate_search">
                <!-- <filter name="Available" string="Available" domain="[('type','=','Available')]"
                />  -->

                <field name="name" string="Title" />
                <field name="postcode" />
                <field name="property_type_id" />
                <field name="expected_price" />
                <field name="bedrooms" />
                <field name="facades" />
                <field name="living_area" filter_domain="[('living_area', '>=', self)]" />
                <field name="garage" />
                <separator />
                <filter string="Available" name="available"
                    domain="[('state','in',('new','offer_received'))]" />
                <group string="Group By">
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}" />
                </group>
            </search>
        </field>
    </record>
</odoo>